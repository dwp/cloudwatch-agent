resources:
  - name: cloudwatch-agent-pr
    type: pull-request
    source:
      repository: dwp/cloudwatch-agent
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: cloudwatch-agent
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/cloudwatch-agent.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h
